<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonTabStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:button">@null</item>
        <item name="android:textSize">@dimen/home_txt_button</item>
        <item name="android:textColor">@drawable/btn_txt_selector</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="TitleStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/home_title_text_size</item>
        <item name="android:textColor">@color/new_card_text_color</item>
        <item name="android:layout_marginTop">@dimen/home_text_margin_top</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="LineStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_below">@+id/tv_title</item>
        <item name="android:layout_marginTop">@dimen/home_text_margin_bottom</item>
        <item name="android:background">@color/home_title_color</item>

    </style>

</resources>
